[workspace]
resolver = "2"
members = [
    "wacp/crates/*",
    "wacp/tests",
    "wacp-console/crates/*",
]

[workspace.package]
edition = "2024"
version = "0.1.0"
license = "Apache-2.0"
repository = "https://github.com/Madahub-dev/wacp-platform"

[workspace.dependencies]
# Async runtime
tokio = { version = "1", features = ["full"] }
tokio-util = { version = "0.7", features = ["rt"] }
tokio-stream = "0.1"
futures = "0.3"

# Web framework
axum = { version = "0.8", features = ["ws"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["cors", "trace", "compression-gzip"] }

# gRPC / Protobuf
tonic = "0.13"
prost = "0.13"
tonic-build = "0.13"

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml_ng = "0.10"

# Storage
sqlx = { version = "0.8", features = ["runtime-tokio", "tls-rustls", "sqlite", "uuid", "chrono"] }
rusqlite = { version = "0.32", features = ["bundled"] }

# Concurrency
arc-swap = "1"
parking_lot = "0.12"

# CLI
clap = { version = "4", features = ["derive"] }

# Error handling
thiserror = "2"

# HTTP client
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "json", "stream"] }

# Regex
regex = "1"

# JSON Schema
jsonschema = "0.28"

# Identifiers
uuid = { version = "1", features = ["v4", "serde"] }

# Time
chrono = { version = "0.4", default-features = false, features = ["serde", "clock"] }

# Cryptography
argon2 = "0.5"
sha2 = "0.10"
subtle = "2"
rand = "0.9"
base64 = "0.22"
ring = "0.17"

# Compression
zstd = "0.13"

# Observability
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["json", "env-filter", "fmt"] }
prometheus = "0.14"

# Filesystem
directories = "6"

# OpenAPI
utoipa = { version = "5", features = ["axum_extras"] }

# Embedding
rust-embed = "8"

# Internal workspace members — consumers inherit via `{ workspace = true }`.
# Paths declared here so cargo resolves to the local member; per-crate
# Cargo.tomls reference these via `{ workspace = true }` rather than
# duplicating paths.
wacp-clock = { path = "wacp/crates/wacp-clock" }
wacp-coordinator = { path = "wacp/crates/wacp-coordinator" }
wacp-coordinator-sdk = { path = "wacp/crates/wacp-coordinator-sdk" }
wacp-fsm = { path = "wacp/crates/wacp-fsm" }
wacp-permissions = { path = "wacp/crates/wacp-permissions" }
wacp-proto = { path = "wacp/crates/wacp-proto" }
wacp-recovery = { path = "wacp/crates/wacp-recovery" }
wacp-sdk = { path = "wacp/crates/wacp-sdk" }
wacp-taxonomy = { path = "wacp/crates/wacp-taxonomy" }
wacp-tools = { path = "wacp/crates/wacp-tools" }
wacp-trail = { path = "wacp/crates/wacp-trail" }
wacp-transport = { path = "wacp/crates/wacp-transport" }
wacp-types = { path = "wacp/crates/wacp-types" }
wacp-workspace = { path = "wacp/crates/wacp-workspace" }
console-api = { path = "wacp-console/crates/console-api" }
console-core = { path = "wacp-console/crates/console-core" }
console-db = { path = "wacp-console/crates/console-db" }
console-runtime = { path = "wacp-console/crates/console-runtime" }
console-test-support = { path = "wacp-console/crates/console-test-support" }

# Test dependencies
rstest = "0.25"
insta = { version = "1", features = ["yaml"] }
tempfile = "3"

# WSL2 memory-pressure profiles (mirrors both source workspaces — identical blocks).
[profile.dev]
codegen-units = 512
debug = "line-tables-only"

[profile.test]
codegen-units = 512
debug = "line-tables-only"
